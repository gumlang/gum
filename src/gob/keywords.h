#ifndef GUM_KEYWORDS_H_
#define GUM_KEYWORDS_H_

#define GUM_KEYWORDS(callback) \
	callback(import) \
	callback(static) \
	callback(native) \
	callback(set) \
	callback(get) \
	callback(as) \
	callback(is) \
	callback(if) \
	callback(else) \
	callback(while) \
	callback(for) \
	callback(in) \
	callback(continue) \
	callback(break) \
	callback(return) \
	callback(false) \
	callback(true)

#endif